#!/bin/sh

active=$(ps -ef | grep X | grep -v grep | awk {'print $9'})
echo "starting on $active"
DISPLAY=$active /usr/bin/google-chrome --allow-file-access-from-files --disable-bundled-ppapi-flash &
